{
  "private": true,
  "version": "0.5.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "version:set": "node scripts/version.mjs"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
