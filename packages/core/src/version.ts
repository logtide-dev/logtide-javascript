/**
 * SDK identity stamped on every entry as `metadata.sdk` (spec 003 §3).
 * Keep SDK_VERSION in sync with package.json on release.
 */
export const SDK_NAME = 'logtide-javascript';
export const SDK_VERSION = '0.8.0';
